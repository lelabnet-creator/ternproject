# Keep the dependency tree strict: undeclared imports must fail loudly rather
# than resolve through hoisting, which is what keeps the workspace boundaries real.
auto-install-peers=true
strict-peer-dependencies=false
link-workspace-packages=true
