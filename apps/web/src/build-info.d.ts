/**
 * Injected by Vite's `define` at build time — see `vite.config.ts`.
 * Declared rather than imported: `define` performs a textual substitution, so
 * these identifiers exist to the bundler and to nothing else.
 */
declare const __TERN_VERSION__: string
declare const __TERN_BUILD__: string
